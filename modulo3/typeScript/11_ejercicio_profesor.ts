const profesor: { nombre: string; catedra: number; horario: string } = {
  nombre: "Francisco",
  catedra: "PROGRAMACION",
  horario: "2 pm"
};

console.log(persona.nombre);
console.log(persona.edad);
console.log(`Profesor ${nombre}, Master en ${catedra}, a las ${horario}`);